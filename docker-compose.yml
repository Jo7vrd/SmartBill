services:
  db:
    image: postgres:17-alpine
    container_name: smartbill-db
    restart: always
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    ports:
      - "${DB_PORT}:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build: 
      context: ./apps/backend/smartbill-backend
      dockerfile: Dockerfile
    container_name: smartbill-backend
    ports:
      - "${PORT}:8080"
    environment:
      - DB_URL=postgres://${DB_USER}:${DB_PASSWORD}@db:5432/${DB_NAME}?sslmode=disable
      - JWT_SECRET=${JWT_SECRET}
    volumes:
      - ./apps/backend/smartbill-backend:/app
    depends_on:
      db:
        condition: service_healthy

  frontend:
    build:
      context: ./apps/frontend/smartbill-ui
      dockerfile: Dockerfile
    container_name: smartbill-frontend
    ports:
      - "5173:5173"
    volumes:
      - ./apps/frontend/smartbill-ui:/app
      - /app/node_modules
    depends_on:
      - backend

  # 🌟 TAMBAHAN: Engine LLM Lokal
  ollama-engine:
    image: ollama/ollama:latest
    container_name: smartbill-ollama
    restart: unless-stopped
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama

  ai-worker:
    build:
      context: ./apps/ai-worker
      dockerfile: Dockerfile
    container_name: smartbill-ai-worker
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - ./apps/ai-worker:/app
    depends_on:
      - ollama-engine # 🌟 AI Worker bakal nunggu Ollama jalan duluan

volumes:
  db_data:
  ollama_data: # 🌟 TAMBAHAN: Biar file model Qwen nggak hilang pas restart